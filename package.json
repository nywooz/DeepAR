{
  "name": "my-project",
  "source": [
    "public/index.html"
  ],
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel public/index.html",
    "prebuild": "rm -rf .parcel-cache",
    "build": "parcel build"
  },
  "devDependencies": {
    "parcel": "^2.9.2"
  },
  "dependencies": {
    "deepar": "^5.2.0-chromeExtension"
  }
}